# Preset Definitions

Markdown-only reference docs for item-related preset values.

- `item-types-definition.md`: canonical `item_type` values + examples for each type.
- `rarity-definition.md`: canonical rarity strings + rank/multiplier map.
- `elements-definition.md`: canonical element ids + interaction notes.
